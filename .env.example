# Copy this file to .env.local and fill with your Supabase project values
# Do NOT commit your real keys to version control.

# Supabase (client) - used by browser/server to access public APIs
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase (service) - optional server-only key for privileged operations
# Store this in your deployment's secret manager (Vercel/Netlify/Env) and
# reference it in server-side code when needed. Do NOT expose it to the client.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# OpenAI / other keys
OPENAI_API_KEY=your_openai_api_key_here

# Storage encryption key (base64). Must match the key used by lib/storage.
STORAGE_KEY=base64:YOUR_BASE64_KEY_HERE
# ==============================================
# APTYS - Configuration Environment Variables
# ==============================================
# Copier ce fichier en .env.local et remplir les valeurs

# ----------------------------------------------
# SUPABASE (Base de données PostgreSQL)
# ----------------------------------------------
# Obtenir sur : https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# ----------------------------------------------
# OPENAI (IA médicale - GPT-4o)
# ----------------------------------------------
# Obtenir une clé API sur : https://platform.openai.com/api-keys
# Coût estimé : ~$0.015 par analyse complète
# Créer un compte et ajouter des crédits (minimum $5 recommandé)
OPENAI_API_KEY=sk-proj-your-key-here

# ----------------------------------------------
# STORAGE & SECURITY
# ----------------------------------------------
# Clé pour chiffrement local des fichiers (AES-256-GCM)
# Générer avec : node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
STORAGE_KEY=base64:REPLACE_WITH_32_BYTE_BASE64_KEY

# Secret pour génération des tokens de partage
# Générer avec : node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SHARE_TOKEN_SECRET=replace_with_random_secret

# ----------------------------------------------
# OPTIONAL: Anthropic Claude (Alternative à OpenAI)
# ----------------------------------------------
# Si vous préférez utiliser Claude 3.5 Sonnet à la place de GPT-4o
# ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# ----------------------------------------------
# SERVER
# ----------------------------------------------
PORT=3000

# ----------------------------------------------
# NOTES
# ----------------------------------------------
# - Ne JAMAIS commiter .env.local dans Git !
# - Garder ces clés secrètes
# - En production, utiliser les variables d'environnement du hosting (Vercel/Netlify)
# - Redémarrer le serveur après modification : npm run dev
